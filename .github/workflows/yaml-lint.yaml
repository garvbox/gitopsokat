name: Lint Yaml

on:
  pull_request:
    branches:
      - main

jobs:
  yamllint:
    name: Check Yaml
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v3
      - uses: ibiqlik/action-yamllint@v3
