---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: wizarr
  namespace: default
spec:
  values:
    ingress:
      main:
        className: traefik
        annotations:
          hajimari.io/icon: mdi:wizard-hat
    persistence:
      data:
        existingClaim: wizarr-data
        type: null
        accessMode: null
        size: null
