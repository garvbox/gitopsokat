---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: whoami
  namespace: default
spec:
  values:
    controllers:
      main:
        replicas: 1
    ingress:
      main:
        className: traefik
        annotations:
          hajimari.io/icon: simple-icons:traefikproxy
