---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: seerr
  namespace: default
spec:
  values:
    persistence:
      config:
        existingClaim: seerr-config
        type: null
        accessMode: null
        size: null
