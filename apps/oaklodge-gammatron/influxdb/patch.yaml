---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: influxdb
  namespace: default
spec:
  values:
    persistence:
      influxdata:
        existingClaim: influxdb-data
        type: null
        accessMode: null
        size: null
