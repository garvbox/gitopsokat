---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: whoami
  namespace: default
spec:
  values:
    ingress:
      main:
        annotations:
          traefik.ingress.kubernetes.io/router.middlewares: default-whitelist@kubernetescrd
